// Shared between server-only drafting code and client review-UI
// components — deliberately NOT under "server-only" so
// EditableScreeningAnswers can compare against it to detect an unfilled
// placeholder field.
export const PLACEHOLDER_TEXT = "[Fill in yourself — this requires your own judgment call]";
